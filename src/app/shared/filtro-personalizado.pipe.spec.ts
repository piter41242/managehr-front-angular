import { FiltroPersonalizadoPipe } from './filtro-personalizado.pipe';

describe('FiltroPersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
