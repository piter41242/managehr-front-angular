import { FiltroHistorialPipe } from './filtro-historial.pipe';

describe('FiltroHistorialPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHistorialPipe();
    expect(pipe).toBeTruthy();
  });
});
